seed: 42
encoder_name: taide/Llama3-TAIDE-LX-8B-Chat-Alpha1
batch_size: 1
max_length: 512
device: cuda
use_instruct: false

svr:
  kernel: rbf
  C: 10.0
  epsilon: 0.2

data:
  train_path: data/processed/train.csv
  dev_path: data/processed/dev.csv
  test_path: data/processed/test.csv
  embeddings_dir: data/embeddings
  results_dir: results
