# Raw data

Put datasets here (not committed).

## Shared task (dev / test)

Clone if missing:

```
git clone https://github.com/NYCU-NLP/ROCLING-2025-ST-DSA-MST.git
```

Train (CVAT) and labeled validation already live under `ROCLING-2025-ST-DSA-MST/Dataset/`.

```
python -m src.prepare_data
```
